services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
    depends_on:
      - backend
    networks:
      - app-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      DATABASE_URL: postgresql://productivity:***REMOVED_POSTGRES_PASSWORD***@db:5432/productivity
    networks:
      - app-network
    depends_on:
      - db

  db:
    image: postgres:13-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      POSTGRES_USER: productivity
      POSTGRES_PASSWORD: ***REMOVED_POSTGRES_PASSWORD***
      POSTGRES_DB: productivity
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  postgres_data: